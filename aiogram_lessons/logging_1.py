import logging
import sys

# #define formatting styles
# format1 = '#%(levelname)-8s [%(asctime)s] - %(filename)s:'\
#            '%(lineno)d - %(name)s - %(message)s'
# format2 = '[{asctime}] #{levelname:8} {filename}:'\
#            '{lineno} - {name} - {message}'
#
# #initialyze formatters
# formatter1 = logging.Formatter(format1)
# formatter2 = logging.Formatter(format2, style='{')
#
# #create logger
# logger = logging.getLogger(__name__)
#
# #initialyze handlers
# stderr_handler = logging.StreamHandler()
# stdout_handler = logging.StreamHandler(sys.stdout)
#
# #set formatters for handlers
# stderr_handler.setFormatter(formatter1)
# stdout_handler.setFormatter(formatter2)
#
# #add handlers to logger
# logger.addHandler(stderr_handler)
# logger.addHandler(stdout_handler)
#
# #create log
# logger.warning('Warning message')

